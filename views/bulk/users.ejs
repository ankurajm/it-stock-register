<% title = 'Bulk Import Users' %>
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="fw-bold mb-0"><i class="bi bi-upload me-2"></i> Bulk Import Users</h4>
        <small class="text-muted">Import multiple users from a CSV file</small>
    </div>
    <a href="/users" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Back</a>
</div>

<% if (success && results && results.length > 0) { %>
<div class="alert alert-success"><i class="bi bi-check-circle me-1"></i> <%= success %></div>
<div class="card mb-4">
    <div class="card-header bg-success text-white"><i class="bi bi-person-badge me-1"></i> Generated Credentials</div>
    <div class="table-responsive">
        <table class="table table-sm mb-0">
            <thead class="table-light">
                <tr><th>#</th><th>Username</th><th>Password</th><th>Initials</th></tr>
            </thead>
            <tbody>
                <% results.forEach((r, i) => { %>
                <tr>
                    <td><%= i + 1 %></td>
                    <td><code><%= r.username %></code></td>
                    <td><code><%= r.password %></code></td>
                    <td><code><%= r.initials %></code></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</div>
<% } else if (success) { %>
<div class="alert alert-success"><i class="bi bi-check-circle me-1"></i> <%= success %></div>
<% } %>

<% if (preview && preview.tempFile) { %>
<div class="card mb-4 border-primary">
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
        <span><i class="bi bi-eye me-1"></i> Preview — <%= preview.rows.length %> row(s)</span>
        <span class="badge bg-light text-primary"><%= preview.rows.filter(r => !r.errors.length && !r.duplicate).length %> valid</span>
    </div>
    <div class="table-responsive" style="max-height:400px">
        <table class="table table-sm table-bordered mb-0">
            <thead class="table-light sticky-top">
                <tr>
                    <th>#</th>
                    <% preview.headers.forEach(h => { %>
                    <th><%= h %></th>
                    <% }); %>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                <% preview.rows.forEach((r, i) => { %>
                <tr class="<%= r.errors.length ? 'table-danger' : r.duplicate ? 'table-warning' : 'table-success' %>">
                    <td><%= i + 1 %></td>
                    <% preview.headers.forEach(h => { %>
                    <td><%= r.data[h] || '-' %></td>
                    <% }); %>
                    <td>
                        <% if (r.errors.length) { %>
                        <span class="text-danger" title="<%= r.errors.join('; ') %>"><i class="bi bi-x-circle"></i> errors</span>
                        <% } else if (r.duplicate) { %>
                        <span class="text-warning"><i class="bi bi-exclamation-triangle"></i> duplicate</span>
                        <% } else { %>
                        <span class="text-success"><i class="bi bi-check-circle"></i> ready</span>
                        <% } %>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <div class="card-footer bg-light">
        <form method="POST" action="/bulk/users">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <input type="hidden" name="confirm" value="1">
            <input type="hidden" name="_tempFile" value="<%= preview.tempFile %>">
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg me-1"></i> Confirm Import</button>
                <a href="/bulk/users" class="btn btn-outline-secondary"><i class="bi bi-x-lg me-1"></i> Cancel</a>
            </div>
        </form>
    </div>
</div>
<% } else if (preview) { %>
<div class="card mb-4 border-danger">
    <div class="card-header bg-danger text-white">
        <i class="bi bi-exclamation-triangle me-1"></i> Validation Errors — <%= preview.rows.length %> row(s)
    </div>
    <div class="table-responsive" style="max-height:400px">
        <table class="table table-sm table-bordered mb-0">
            <thead class="table-light sticky-top">
                <tr>
                    <th>#</th>
                    <% preview.headers.forEach(h => { %>
                    <th><%= h %></th>
                    <% }); %>
                    <th>Errors</th>
                </tr>
            </thead>
            <tbody>
                <% preview.rows.forEach((r, i) => { %>
                <tr class="<%= r.errors.length ? 'table-danger' : r.duplicate ? 'table-warning' : 'table-success' %>">
                    <td><%= i + 1 %></td>
                    <% preview.headers.forEach(h => { %>
                    <td><%= r.data[h] || '-' %></td>
                    <% }); %>
                    <td>
                        <% if (r.errors.length) { %>
                        <span class="text-danger"><%= r.errors.join('; ') %></span>
                        <% } else if (r.duplicate) { %>
                        <span class="text-warning">duplicate username</span>
                        <% } else { %>
                        <span class="text-success">ok</span>
                        <% } %>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <div class="card-footer">
        <a href="/bulk/users" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i> Fix &amp; Re-upload</a>
    </div>
</div>
<% } %>

<div class="card">
    <div class="card-body p-4">
        <h5 class="fw-semibold mb-3">CSV Format</h5>
        <div class="alert alert-info">
            <strong>Required column:</strong> <code>username</code><br>
            <strong>Optional columns:</strong> <code>password</code> (auto-generated if empty), <code>initials</code> (auto-generated from name if empty), <code>role</code> (user/admin)
        </div>

        <div class="d-flex gap-2 mb-3">
            <a href="/bulk/template/users" class="btn btn-sm btn-outline-success"><i class="bi bi-download me-1"></i> Download CSV Template</a>
        </div>
        <div class="bg-light rounded p-3 mb-4">
            <small class="text-muted d-block mb-2">Example CSV (password auto-generated if omitted):</small>
            <code style="font-size:12px">username,initials,role<br>john,JDU,user<br>jane,JDO,user</code>
        </div>

        <% if (error) { %>
        <div class="alert alert-danger"><i class="bi bi-exclamation-triangle me-1"></i> <%= error %></div>
        <% } %>

        <form method="POST" action="/bulk/users" enctype="multipart/form-data">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="mb-3">
                <label class="form-label">Select CSV File <span class="text-danger">*</span></label>
                <input type="file" name="file" class="form-control" accept=".csv" required>
            </div>
            <button type="submit" class="btn btn-primary"><i class="bi bi-upload me-1"></i> Preview &amp; Import</button>
        </form>
    </div>
</div>